{
    "template": "xfes_index",
    "description": "Redact version if required",
    "execution_order": 10,
    "enabled": true,
    "action": "preg_replace",
    "find": "#<xf:formrow[^>]+xfes_version[^>]+>.*?</xf:formrow>#si",
    "replace": "<xf:if is=\"$canViewServerInfo\">$0</xf:if>"
}